services:
  jekyll:
    image: jekyll/jekyll:latest
    platform: linux/amd64
    command: >
      sh -c "gem install webrick && jekyll serve --watch --force_polling --verbose"
    ports:
      - 4000:4000
    volumes:
      - .:/srv/jekyll